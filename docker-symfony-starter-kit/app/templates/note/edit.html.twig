{% include 'default/_form.html.twig' with {
    page_title: 'Edit the note'|trans({'%id%': note.id|default('')}),
    form: form,
    submit_label: 'Save'|trans,
    back_to_list_path: 'note_index',
} %}